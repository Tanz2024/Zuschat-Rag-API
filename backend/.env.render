# Render PostgreSQL Environment Configuration
# Copy this to your .env file and replace with your actual Render database URL

# Render PostgreSQL Connection (REPLACE WITH YOUR ACTUAL URL FROM RENDER DASHBOARD)
# Get this from: Render Dashboard > Your PostgreSQL Service > External Database URL
DATABASE_URL=postgresql://username:password@dpg-xxxxx-a.oregon-postgres.render.com/database_name

# 📋 INSTRUCTIONS:
# 1. Create PostgreSQL on Render
# 2. Copy the External Database URL from Render dashboard
# 3. Replace the DATABASE_URL above with your real URL
# 4. Copy this file to backend/.env

# Database Pool Settings (Optimized for Render)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=60
DB_ECHO=false

# Application Configuration
MAX_CONVERSATION_HISTORY=20
SESSION_TIMEOUT_HOURS=2
DEBUG=false
LOG_LEVEL=INFO

# Example Render Database URLs:
# External: postgresql://zuschat_user:xyz123@dpg-abc123-a.oregon-postgres.render.com/zuschat_db
# Internal: postgresql://zuschat_user:xyz123@dpg-abc123-a:5432/zuschat_db
